# LICENA — плеер курса: «Retake wrong only» — пересдача только ошибочных вопросов блока (отчёт для владельца)

Последняя сверка: 2026-09-11
Статус: **READY_FOR_REVIEW** (после дополнений 2026-09-11: кнопка в сайдбаре `75e901f` — §9; пересдача сохраняется
и возобновляется, `c2c0263` — §10; пропущенные вопросы засчитываются как ошибки, `564182a` — §11).
Merge в `main` и production deploy НЕ выполнялись — только после явного `APPROVED_FOR_MAIN`.

## 0. Откуда задача

Запрос пользователя продукта через чат приложения → issue `lalianamen/LLICENA#189` (метки `support`,
`request`; курс `c8-concrete`, locale `en`): «сейчас приходится пересдавать весь блок из 100 вопросов;
нужен режим пересдачи только пропущенных». Карточки задачи в `tasks/` нет — владелец дал ход напрямую в
сессии 2026-09-11 («да подготовь это»); метка `from-chat` на issue не ставилась. Персональные данные из
issue (email, ticket id) в этот репозиторий не переносятся.

Проверка до правки (чтение `js/app-course.js` на `main` `9a8a577`): после ответа варианты вопроса
блокируются (`renderQ`: `btn.disabled = true`; `pickAnswer` игнорирует повторный выбор); «Try again»
на карточке результата и «Restart» в сайдбаре вызывают один и тот же `resetBlockRun`, который после
подтверждения стирает все ответы блока; фильтр «Wrong only» (`#filterWrongBtn`) только показывает
ошибочные и неотвеченные вопросы в режиме просмотра. Запрос пользователя решён не был.

## 1. Ветка и коммит

Репозиторий `lalianamen/llicena`, ветка `claude/cool-maxwell-dw6toy`, запушена.
База — `main` @ `9a8a577` (2026-09-04). Один коммит — `08ef240` (полный:
`08ef24059abc235b764a220a5fa33c8f0c73c707`). Diff: 6 файлов, +51/−10.
`main` основного репозитория не изменён; production не задеплоен.

## 2. Что сделано

### 2.1. Поведение
- На карточке результата блока (`#resultsCard`) появилась кнопка `#retakeWrongBtn` —
  «Retake wrong only (N)» / «Repetir solo los errores (N)» / «Пересдать только ошибки (N)», где N —
  число ошибочных ответов в активном блоке. Показывается только при N > 0; при 100 % — скрыта.
  Стиль `btn-gold` (акцентная CTA дизайн-системы, как «Finish & submit» в Final Exam); «Try again»
  остаётся `btn-primary`. Обе кнопки — в новом ряду `.result-actions` (flex, перенос на 360 px).
- Клик → `retakeWrong()`: из `lp:answers:<course>` удаляются ТОЛЬКО ошибочные ответы активного блока
  (`scopeWrongIds()`), правильные сохраняются; варианты стёртых вопросов перемешиваются заново
  (`clearOptPerms`); `activeSection = null`; включается существующий фильтр `filterWrong`; порядок
  перетасовывается (`resetOrder(true)`). Прогон идёт только по ещё не решённым вопросам блока:
  «Question 1 of N», прогресс «0 / N». Подтверждение не запрашивается (стираются только ошибки).
- Запись `lp:runs` (прошлый прогон блока для сравнения) при пересдаче НЕ создаётся: это запись
  о полном прогоне. Сравнение «Last run» на карточке результата теперь показывается только при
  `prev.total === total` — иначе результат подмножества (пересдача, секция) сравнивался бы с полным
  прогоном и давал бессмысленную дельту.
- Пересдачу можно повторять: после прогона по 10 вопросам с 3 ошибками карточка результата
  предлагает «Retake wrong only (3)»; когда ошибок нет — кнопка скрыта.

### 2.2. Что НЕ менялось
- «Try again» / «Restart» (`resetBlockRun`) — по-прежнему сброс всего блока с подтверждением и
  архивацией прогона.
- Сайдбарный фильтр «Wrong only» — по-прежнему только просмотр (варианты отвеченных вопросов
  заблокированы). Во время пересдачи он включён и показывает «✓ Wrong only (M)», где M — текущее
  число ошибочных ответов по всему курсу (после стирания ошибок блока это может быть 0) — семантика
  метки не менялась, см. §8.
- Final Exam (таймер, набор вопросов, результат, review) — не затронут: пересдача только ошибок
  противоречила бы «условиям экзамена».
- Схема Supabase, RLS, Edge Functions, платежи, аналитика, маршруты — не тронуты. Новых событий
  аналитики нет (в `js/app-course.js` вызовов `track()` нет и не было).
- Ключи localStorage (`lp:answers`, `lp:optperm`, `lp:order`, `lp:runs`, `lp:summary`) — те же;
  сводка для кабинета (`updateCourseSummary`) обновляется через `saveAnswers()` как при любом ответе.

## 3. Официальные источники

Не применимо: изменение — механика плеера; содержимое банков, ключи, переводы вопросов и логика
экзаменов не менялись.

## 4. Изменённые файлы

`js/app-course.js` (v70): `scopeWrongIds()`, `retakeWrong()`, дополнение `showResults()` (кнопка + условие
сравнения), обработчик клика. `js/i18n-app.js` (v58): ключ `courseRetakeWrong` в EN/ES/RU. `course.html`,
`preview.html`: разметка карточки результата (ряд `.result-actions`, кнопка) + бампы. `css/course.css`
(v36): `.result-actions`. `app.html`: бамп `?v=` у `i18n-app.js` (третья страница, которая его грузит).
Новых `id`/`class`: `retakeWrongBtn`, `result-actions`; существующие хуки не переименовывались и не удалялись.

## 5. Проверки (на `08ef240`)

Проект — vanilla HTML/CSS/JS без сборки; «build» = `node --check` внутри `node scripts/verify.js`.

| Проверка | Результат |
|---|---|
| `node scripts/verify.js` | 0 ошибок / 138 файлов; paid-list sync 6 списков / 24 курса; course-ref 30; offer 63 страницы |
| Playwright, `preview.html?id=osha-construction` (реальный плеер, Supabase-заглушка `js/preview-stub.js`), блок 1 (50 вопросов), засеяно 40 верных / 10 ошибочных | EN 1280: 22 функциональные проверки PASS |
| то же, RU 360 px | 22 PASS, без горизонтального скролла |
| то же, ES 390 px | 22 PASS, без горизонтального скролла |
| Сценарий каждого прогона | 1/50 → Next до результата 80 % «40 of 50», «Last run: 30/50 (+10)» показан, кнопка «(10)» видна → клик → «1 / 10», прогресс «0 / 10», фильтр активен, варианты кликабельны, в хранилище 40 верных ответов сохранены → 7 верных + 3 ошибки → «7 of 10», «Last run» скрыт, кнопка «(3)» → клик → «1 / 3» → все верно → 100 %, кнопка скрыта, карточка блока 50/50 → «Try again» (confirm принят) → «1 / 50», прогресс 0, фильтр выключен |
| Ошибки консоли | от кода сайта — 0; три `net::ERR_CONNECTION_RESET` — внешние хосты, заблокированные прокси песочницы (fonts.googleapis.com, googletagmanager.com, clarity.ms) |

Недоступные проверки (честно): typecheck/unit-фреймворка в проекте нет; платный банк (`c8-concrete`,
только из Supabase `bank_questions`) не открывался — нет сессии/доступа к БД; логика не зависит от
источника банка (`QUESTIONS` в памяти, ответы в localStorage), но прогон на платном курсе под реальным
аккаунтом — за владельцем. Тестовые сюиты — scratchpad сессии (`retake-e2e.mjs`), в репозиторий не входят.

## 6. Миграции и совместимость

Новых SQL нет. Формат сохранённых данных не менялся: старые ответы, порядки и записи прогонов
читаются как раньше; у пользователей без ошибок в блоке ничего не появляется. Rollback = revert `08ef240`.
Кэш: бампы `?v=` на всех страницах, которые грузят изменённые файлы (`course.html`, `preview.html`, `app.html`).

## 7. Превью и скриншоты

Превью exact commit `08ef240` (не production): https://claude.ai/code/artifact/7539dad8-6b50-49a8-ae4a-c0182fe50013 —
`preview.html` с бесплатным банком `osha-construction`, собранный в один файл (локальные CSS/JS и три
файла банка встроены; `?id=` закреплён; аналитика и `<meta>` CSP страницы из сборки убраны — у хоста
артефактов своя политика). Чтобы увидеть кнопку: ответить на все 50 вопросов блока с хотя бы одной ошибкой
(или переключиться на другой блок) — карточка результата появится после последнего «Next». Ссылки «My courses»,
иконки и Service Worker в одном файле не работают — это ограничение сборки, не продукта.

Скриншоты (`tasks/reports/img/`): `2026-09-11-retake-results-en.png` (карточка результата 40/50 с новой кнопкой,
EN 1280), `2026-09-11-retake-run-en.png` (прогон «Question 1 of 10», фильтр активен), `2026-09-11-retake-results-ru-360.png`
(RU 360: «Пересдать только ошибки (10)» и «Начать заново» в столбик), `2026-09-11-retake-second-es-390.png`
(ES 390: результат пересдачи 7/10, «Repetir solo los errores (3)», без «Last run»).

## 8. Нерешённые вопросы (для владельца)

1. Метка сайдбарного фильтра «✓ Wrong only (M)» во время пересдачи показывает число ошибок по всему
   курсу (после стирания ошибок блока — часто «(0)»), хотя прогон идёт по N вопросам. Семантика метки
   существующая, не менялась; можно показывать число вопросов в текущем прогоне — отдельное решение.
2. Ярлык результата пересдачи («✓ Passed!» при ≥ 70 % от подмножества) считается по прогону, как и для
   секций сегодня; отдельного ярлыка «пересдача» нет.
3. Перезагрузка страницы посреди пересдачи: фильтр «Wrong only» не сохраняется (существующее поведение) —
   плеер откроет полный порядок блока на первом неотвеченном вопросе; стёртые ответы при этом не теряются.
4. Метка `from-chat` на issue #189 и ответ пользователю через тикет — за владельцем.

## 9. Дополнение 2026-09-11 (позже): кнопка пересдачи доступна в любой момент, отдельно для каждого блока

Замечание владельца по превью `08ef240`: если после прохождения блока не нажать пересдачу сразу, потом её не
найти. Ошибочные ответы при этом сохранялись (localStorage), но кнопка жила только на карточке результата,
которая появляется после последнего вопроса прогона; при возврате в курс блок открывается на первом вопросе,
и до карточки нужно долистать весь блок. Указание владельца: «сделай кнопку доступной», «отдельно для каждого
блока». Второй коммит в той же ветке — `75e901f` (полный `75e901f1ffd8d94b7eeac1710a0e71d010b616e2`), поверх
`08ef240`; `main` не изменён, не задеплоено.

Сделано:
- В сайдбаре (`.side-controls`, между «Wrong only» и «Restart») новая кнопка `#retakeWrongSideBtn`
  «Retake wrong only (N)» с иконкой, той же строкой `courseRetakeWrong` EN/ES/RU и тем же `retakeWrong()`.
  N и действие — по АКТИВНОМУ блоку: переключение карточки блока меняет счётчик; при N = 0 кнопка отключена
  (`updateRetakeWrongBtn`, вызывается из `updateFilterWrongBtn`, т. е. при каждом ответе, смене блока/секции,
  пересдаче и смене языка). Ответы других блоков пересдача не трогает. После клика из сайдбара карточка вопроса
  подводится под липкую шапку (`scrollToCardTop`) — на телефонах сайдбар лежит ниже карточки.
- Единственное касание существующего кода: `wrongCount()` (метка фильтра «Wrong only (N)») теперь считает
  ошибки активного блока, а не всего курса. Сам фильтр и раньше сужал порядок только текущего блока, так что
  метка «14» при 10 отфильтрованных вопросах была неточной; рядом с новой кнопкой два разных числа читались
  как противоречие (см. скриншот до правки в истории сессии: «Wrong only (14)» / «Retake wrong only (10)»).
  Для банков без блоков (`activeBlock === null`) значение прежнее — весь курс. Пункт 1 раздела §8 этим закрыт.
- CSS `.ctrl-btn-retake` (`:disabled` как у фильтра; на телефонах — на всю ширину ряда, как фильтр).
  Бампы: `js/app-course.js` v71, `css/course.css` v37 (`course.html`, `preview.html`). Строки не менялись.

Проверки (на `75e901f`): `node scripts/verify.js` 0 ошибок / 138 файлов. Playwright, `preview.html?id=osha-construction`:
новый сценарий «возврат» (блок 1 засеян 40 верных / 10 ошибок, блок 2 — 6 ответов, из них 4 ошибки): при входе блок 1
открывается на «1 / 50», в сайдбаре «Retake wrong only (10)» активна → клик → «1 / 10», карточка вопроса в зоне видимости
(desktop top 293 px — страница не прокручивается; RU 360 top 68 px — под шапкой), кнопка стала «(0)» и отключена, в хранилище
у блока 1 40 верных ответов, у блока 2 — все 6 → карточка «Block 2» → «(4)» → клик → «1 / 48» (44 неотвеченных + 4 стёртых),
у блока 2 осталось 2 верных, блок 1 не тронут → назад на блок 1 → кнопка отключена. EN 1280 и RU 360 — 15 проверок PASS
в каждом; прежний сценарий карточки результата (EN 1280 / RU 360 / ES 390, по 23 проверки) — PASS без изменений.
Консоль: ошибок от кода сайта 0; неуспешные запросы только к fonts.googleapis.com / googletagmanager.com / clarity.ms
(прокси песочницы). Горизонтального скролла нет.

Скриншоты: `tasks/reports/img/2026-09-11-retake-sidebar-en.png` (сайдбар с «Wrong only (10)» и «Retake wrong only (10)»
при возврате в блок, EN 1280), `2026-09-11-retake-sidebar-ru-360.png` (RU 360: ряд кнопок сайдбара под карточкой вопроса).
Превью пересобрано из `75e901f` по той же ссылке: https://claude.ai/code/artifact/7539dad8-6b50-49a8-ae4a-c0182fe50013 —
кнопка в сайдбаре видна сразу после первого ошибочного ответа в блоке, карточку результата ждать не нужно.

## 10. Дополнение 2026-09-11 (ещё позже): пересдача сохраняется и возобновляется с места остановки

Замечание владельца по превью `75e901f`: «если я нажал, но не прошёл, то он сбрасывает и к ним больше не возвращается».
Воспроизведено: `retakeWrong()` из `08ef240` СТИРАЛ ошибочные ответы и держал набор пересдачи только в памяти (фильтр
`filterWrong`), поэтому после ухода посреди пересдачи на диске не оставалось сведений о том, какие вопросы пересдавались:
блок открывался на первом вопросе, стёртые вопросы ничем не отличались от никогда не отвеченных, а счётчики показывали «(0)».
В частично пройденном блоке прогон к тому же разрастался до «ошибки + неотвеченные» (в тесте — 48 вместо 4).
Третий коммит в той же ветке — `c2c0263` (полный `c2c02635dd3a6f8615655254395e0e4d407dcc3b`), поверх `75e901f`; `main` не
изменён, не задеплоено.

Новая механика:
- `lp:retake:<course>` = `{ [ключ блока]: [id вопросов, …] }` — список «ещё к пересдаче» по блокам. Вопрос из списка
  СОХРАНЯЕТ свой ошибочный ответ (карточки блоков, сводка для кабинета и счётчик «Wrong only» остаются правдивыми), но
  показывается как неотвеченный, пока на него не ответят снова; ответ удаляет его из списка (`isDone(id)` = «отвечен и не в
  списке» — используется в `renderQ`, `pickAnswer`, `updateProgress` и в логике возобновления; статистика по-прежнему считает
  сохранённые ответы).
- Пока у блока есть список, его прогон «All sections» — ровно этот список: «1 / 10», после перезагрузки — «1 / 7» по
  оставшимся. Прогон в частично пройденном блоке теперь равен числу ошибок («1 / 4»), а не «ошибки + неотвеченные».
- Список сбрасывают «Try again» / «Restart» / «Reset progress» и клик по подсвеченной кнопке «✓ Wrong only» (выход из
  пересдачи: ответы остаются ошибочными, пересдача снова предлагается с текущим числом). Кнопка сайдбара отключена, пока
  идёт пересдача этого блока; в других блоках — активна со своим числом.
- Одноразовая миграция `epa-608` чистит и этот ключ. Бамп `js/app-course.js` v72. Строк, CSS и разметки в этом коммите нет.

Проверки (на `c2c0263`): `node scripts/verify.js` 0 ошибок / 138 файлов; Playwright — 114 проверок PASS в 5 прогонах
(EN 1280 / RU 360 / ES 390): прежний сценарий карточки результата (в хранилище 40 верных И 10 ошибочных, список из 10
записан) и сценарий сайдбара по блокам, дополненный случаем владельца: в пересдаче блока 1 ответить на один вопрос верно →
перезагрузка → «1 / 9», «Wrong only» подсвечен, кнопка сайдбара отключена, вопрос отвечаем → ответить неверно → в списке 8
→ клик по «✓ Wrong only» → «1 / 50», пересдача снова предлагается с «(9)». Ошибок консоли от кода сайта 0; неуспешные запросы
только к внешним хостам, заблокированным прокси песочницы. Расхождение, замеченное по пути (после перезагрузки счётчик ошибок
«возвращался» к 10), оказалось дефектом тестового скрипта — он повторно засевал ответы при каждой загрузке; исправлен
(засев один раз на контекст), продукт вёл себя верно.

Скриншот: `tasks/reports/img/2026-09-11-retake-resumed-en.png` — пересдача блока 1 после перезагрузки: «Question 1 of 9»,
«0 / 9», «✓ Wrong only (9)» подсвечен, «Retake wrong only (9)» отключена. Превью пересобрано из `c2c0263` по той же ссылке:
https://claude.ai/code/artifact/7539dad8-6b50-49a8-ae4a-c0182fe50013 (в артефакте localStorage живёт в браузере зрителя —
перезагрузка страницы сохраняет пересдачу так же, как на сайте).

Пункт 3 раздела §8 (фильтр не переживал перезагрузку) закрыт этим коммитом.

Примечание к превью (2026-09-11, после вопроса владельца «кнопка начать заново не работает?»): в изолированном фрейме
артефакта браузер блокирует `window.confirm()` и молча возвращает «отмена», поэтому «Try again» / «Restart» /
«Reset progress» (все начинаются с подтверждения) в превью выглядели мёртвыми. Это ограничение среды превью, не продукта:
на сайте показывается диалог, после «OK» сброс выполняется (в Playwright диалог принимался и сброс проходил — «1 / 50»).
В сборку превью добавлена заглушка только для превью: `window.confirm` автоматически отвечает «да» и пишет вопрос в консоль;
в код сайта она не входит. Превью пересобрано по той же ссылке; проверено кликом «Try again» без обработчика диалога → «1 / 50».

## 11. Дополнение 2026-09-11 (вечер): пропущенные вопросы засчитываются как ошибки

Замечание владельца по превью `c2c0263`: «правильных 18, неправильных 17 — а где остальные? может, оставшиеся без ответа
тоже указать ошибочными?». Воспроизведено: если дойти до карточки результата, пролистав часть вопросов без ответа, процент
уже считал их как неверные («18 of 50» = 36 %), но «Wrong only» и пересдача считали только отвеченные неверно — 18 + 17 ≠ 50,
и пропущенные нельзя было пересдать. Решение владельца принято: пропуск = ошибка, как на реальном экзамене (Final Exam уже
трактует пустой ответ так же). Четвёртый коммит ветки — `564182a` (полный `564182acd2cf8417db462d9c1b2ff5f129eb677e`),
поверх `c2c0263`; `main` не изменён, не задеплоено.

Сделано:
- `showResults()`: когда прогон дошёл до карточки результата, каждый вопрос прогона без ответа получает в `lp:answers`
  значение `SKIPPED` (`-1`). Оно везде считается ответом, не совпадающим с ключом: карточки блоков, статистика секций,
  сводка для кабинета, «Wrong only (N)», «Retake wrong only (N)», прогресс сайдбара (обновляется сразу: «50 / 50»).
- Пропущенный вопрос показывается заблокированным, с подсвеченным ключом, без отмеченного варианта и со строкой
  «✗ No answer» / «Sin responder» / «Без ответа» над объяснением (`#qNoAnswer`, строка `examReviewNoAnswer` переиспользована;
  CSS `.q-noanswer`). В пересдаче такой вопрос снова открыт для ответа, строка скрыта.
- Пока блок в процессе (карточки результата не было), ничего не помечается — счётчики показывают только отвеченные неверно.
- Файлы: `js/app-course.js` (v73), `course.html`, `preview.html` (элемент `#qNoAnswer`), `css/course.css` (v38). Новых строк нет.

Проверки (на `564182a`): `node scripts/verify.js` 0 ошибок / 138 файлов. Playwright — новый сценарий «пропуски» (18 ответов:
13 верных / 5 неверных, 32 без ответа; Next до результата): в процессе блока счётчики «(5)»; результат «13 of 50»; 32 записи
`-1` в хранилище; «(37)» на карточке результата, в сайдбаре и у фильтра (13 + 37 = 50); карточка блока 13/50; прогресс «50 / 50»;
пропущенный вопрос — ключ подсвечен, вариант не отмечен, варианты заблокированы, «✗ No answer» виден; пересдача «1 / 37»,
вопрос снова отвечаем, строка скрыта — EN 1280 и RU 360 PASS; прежние сюиты (карточка результата ×3, сайдбар по блокам ×2)
PASS; итого 140 проверок PASS в 7 прогонах, ошибок консоли от кода сайта 0, горизонтального скролла нет.

Скриншоты: `tasks/reports/img/2026-09-11-retake-blank-results-en.png` (карточка результата 13/50 с «(37)» и секциями,
помеченными ⚠), `2026-09-11-retake-blank-question-en.png` (пропущенный вопрос: ключ, «✗ No answer», объяснение),
`2026-09-11-retake-blank-results-ru-360.png`. Превью пересобрано из `564182a` по той же ссылке:
https://claude.ai/code/artifact/7539dad8-6b50-49a8-ae4a-c0182fe50013

## Source References

`lalianamen/llicena@08ef240`, `@75e901f`, `@c2c0263`, `@564182a`: `js/app-course.js` (`SKIPPED`, `renderQ`, `pickAnswer`, `resetBlockRun`, `resetAllProgress`,
`resetOrder`, `updateProgress`, `scopeQuestionIds`, `scopeWrongIds`, `loadRetake`, `saveRetake`, `pendingIds`, `isPending`, `isDone`,
`inRetake`, `clearRetake`, `retakeWrong`, `showResults`, `wrongCount`, `updateFilterWrongBtn`, `updateRetakeWrongBtn`), `js/i18n-app.js`,
`course.html`, `preview.html`, `css/course.css`, `app.html`, `js/preview-stub.js`, `js/questions/osha-construction*.js`,
`scripts/verify.js`; `lalianamen/LLICENA#189`; scratchpad-сюита `retake-e2e.mjs`.

## Verification Status

**Partially Verified** — код и поведение проверены чтением и прогоном в песочнице на бесплатном банке;
платный банк под реальным аккаунтом не прогонялся (нет доступа); статус тикета пользователя в Supabase — UNKNOWN.
